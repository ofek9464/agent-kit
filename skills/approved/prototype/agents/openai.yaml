interface:
  display_name: "Prototype"
  short_description: "Answer one design question with a demo"
  default_prompt: "Use $prototype to build a disposable demo that answers this design question."

policy:
  allow_implicit_invocation: false
